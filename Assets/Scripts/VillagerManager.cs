using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Used for NPCs
//Contains dialogue and wandering
//Do dialogue in separate script
//Walks between 2+ waypoints (adapt from John Lemon)
//Hurtboxes = dialogue triggers
//Triggers dialogue when player hitbox collides with object tagged "NPC_Hurtbox"

public class VillagerManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

}
